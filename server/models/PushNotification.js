const sendNewQuestionsToAll = question => {
  // return User.getAllWithNotifications().then(users => {
  //   users.forEach(user => {
  //     // Send the notifications https://github.com/expo/exponent-server-sdk-node
  //   });
  // });
};

const addPushToken = () => {};

module.exports = {
  sendNewQuestionsToAll,
  addPushToken
};
